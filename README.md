# WeatherForecast
Create dockerised .Net Core microservice that has a single API endpoint that serves weather forecast. The input is a date, city, and country. The response should contain collection of forecasts from at least 3 web sites. The service must cache the reponse to avoid unnecessary calls to 3rd party sites. Deploy the app to free tier Azure service
